package OOPs;

public class ClassE {
    public static void main(String[] args) {
        ClassD d = new ClassD(); //object of subclass calls subclass method if superclass and subclass method have sane name
        d.test();
    }
}
